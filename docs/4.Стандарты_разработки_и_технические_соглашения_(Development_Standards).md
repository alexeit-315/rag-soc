# Development Standards & Technical Conventions (MVP)

| **Версия**   | 1.3 (MVP)                                                |
| ------------ |----------------------------------------------------------|
| **Статус**   | В разработке                                             |
| **Дата**     | 07.04.2026                                               |
| **Система**  | Technical Documentation RAG SOC Platform                 |
| **Хранение** | `rag-soc-core/docs/development/development-standards.md` |

---
## 0. Принципы (обязательно прочитать)
   **Скорость важнее идеальности**  
   **Простота > гибкость**  
   **Автоматизируем только то, что реально используем**  
   **Каждое правило должно быть проверяемо в CI**  

---

## 1. Организация кода и репозиториев

### 1.1 Структура репозиториев

Используется **Monorepo**:

| Репозиторий | Описание                                                                                                    | Язык/Технологии              |
|-------------|-------------------------------------------------------------------------------------------------------------|------------------------------|
| `rag-soc-core` | AI-сервисы (конвертеры, индексация, RAG пайплайн) и общие библиотеки, API Gateway, External API, Frontend, Инфраструктура как код (K8s, Istio, Helm, мониторинг) | Python, Java/Kotlin, React, YAML, Helm |

---

### 1.2 Структура `rag-soc-core`

```
rag-soc-core/
├── services/
│   ├── rag-soc-converter/
│   ├── rag-soc-indexer/
│   ├── rag-soc-index-router/
│   ├── rag-soc-embedding/
│   ├── rag-soc-experimentation/
│   ├── rag-soc-evaluation/
│   ├── rag-soc-search/
│   ├── rag-soc-reranker/
│   ├── rag-soc-orchestrator/
│   ├── rag-soc-generator/
│   ├── rag-soc-query-processor/
│   ├── rag-soc-dictionary/
│   ├── rag-soc-dataset/
│   ├── rag-soc-prompt-store/
│   ├── rag-soc-dictionary-store/
│   ├── rag-soc-inference-gateway/
│   ├── rag-soc-config-store/
│   ├── rag-soc-batch/
│   ├── rag-soc-api-gateway/
│   ├── rag-soc-ext-gateway/
│   ├── rag-soc-frontend/
│   ├── rag-soc-infra/
│   └── rag-soc-version-comparison/
├── common/
│   ├── rag-soc-models/
│   ├── rag-soc-clients/
│   ├── rag-soc-logging/
│   └── rag-soc-config/
├── docs/
├── deployments/
├── scripts/
├── .env.example
├── pyproject.toml
├── requirements.txt
├── requirements-dev.txt
├── README.md
└── Makefile
```

> 🔺 **Tier 2/3:** добавить strict разделение domain / application / infra

---

### 1.3 Обязательные файлы для каждого сервиса

```
service/
├── src/
├── tests/
│   ├── unit/
│   └── integration/
├── Dockerfile
├── structure.txt
├── README.md
└── requirements.txt
```

### 1.4 Файл `structure.txt` (пример для rag-soc-core)

```
rag-soc-core/
├── services/
│   ├── rag-soc-converter/
│   ├── rag-soc-indexer/
│   ├── rag-soc-embedding/
│   ├── rag-soc-search/
│   ├── rag-soc-reranker/
│   ├── rag-soc-orchestrator/
│   ├── rag-soc-generator/
│   ├── rag-soc-query-processor/
│   ├── rag-soc-dictionary/
│   ├── rag-soc-batch/
│   └── rag-soc-version-comparison/
├── common/
│   ├── rag-soc-models/
│   ├── rag-soc-clients/
│   ├── rag-soc-logging/
│   ├── rag-soc-config/
│   └── rag-soc-testing/
├── docs/
├── deployments/
├── scripts/
├── .env.example
├── .gitignore
├── pyproject.toml
├── requirements.txt
├── requirements-dev.txt
├── README.md
└── Makefile
```

---

### 1.5 Naming conventions

| Объект | Правило | Пример |
|--------|---------|--------|
| Репозиторий | `rag-soc-<domain>` | `rag-soc-core`, `rag-soc-gateway` |
| Сервис (Python) | `rag-soc-<service>` | `rag-soc-converter`, `rag-soc-orchestrator` |
| Ветка | `<type>/<issue>-<short-name>` | `feature/RAG-42-add-pdf-converter` |
| Типы веток | `feature/`, `bugfix/`, `hotfix/`, `release/` | — |
| Коммиты | Conventional Commits | `feat(converter): add PDF support` |
| Теги | `v<semver>` | `v1.2.3` |
| Docker образ | `harbor.domain/rag-soc/<service>:<version>-<commit-hash>` | `harbor.domain/rag-soc/converter:1.2.3-a1b2c3d` |
| Helm чарт | `rag-soc-<service>` | `rag-soc-converter` |

---

### 1.6 Conventional Commits

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы:**
- `feat`: новая функциональность
- `fix`: исправление бага
- `docs`: документация
- `style`: форматирование, линтеры
- `refactor`: рефакторинг
- `test`: добавление тестов
- `chore`: CI/CD, конфигурация
- `perf`: оптимизация производительности


---

## 2. Технологический стек

### 2.1 Языки программирования

| Компонент | Язык | Версия | Менеджер зависимостей |
|-----------|------|--------|----------------------|
| AI-сервисы (все в `rag-soc-core`) | Python | 3.11+ | pip + requirements.txt |
| API Gateway, External API | Java/Kotlin | Java 17 / Kotlin 1.9+ | Gradle (Kotlin DSL) |
| Frontend | TypeScript/React | 5.0+ / 18+ | npm / pnpm |
| Инфраструктура | YAML, Helm | — | — |

### 2.2 Форматы взаимодействия

| Сценарий | Формат | Спецификация |
|----------|--------|--------------|
| Синхронное (user → API Gateway → services) | REST | OpenAPI 3.0 |
| Синхронное (внутренние микросервисы) | REST | OpenAPI 3.0 |
| Асинхронное (события пайплайна) | Kafka + CloudEvents | JSON over Kafka |
| Machine-readable документы | JSON | Схема v1 |
| Human-readable документы | TXT, MD | UTF-8 |

### 2.3 Версионирование API

- **REST API**: версия в URL: `/api/v1/chat`

### 2.4 Версионирование артефактов

| Артефакт | Формат | Где хранится |
|----------|--------|--------------|
| Docker образ | `harbor.domain/rag-soc/<service>:<semver>-<git-short-hash>` | Harbor |
| Helm чарт | `rag-soc-<service>-<semver>.tgz` | Harbor / Object Storage |
| Модель | `model:name:version` | Object Storage (S3/NFS) |
| Промпт | `prompt:<name>:v<version>` | Feature Store (Postgres) |
| Индекс | `index:<dataset>:v<version>` | Vector DB + Object Storage |
---

## 3. Конфигурация и секреты

### 3.1 Принципы 12-factor app

- Конфигурация через переменные окружения
- Не хранить конфиги и секреты в коде
- Разделять код и конфигурацию

### 3.2 Иерархия конфигурации

```
configs/
├── defaults.yaml           # Значения по умолчанию (в репозитории)
├── staging.yaml            # Тестовый контур
└── prod.yaml               # Продуктивный контур
```

### 3.3 Пример `.env`

**Локальная разработка** — файл `.env` (не в репозитории, в `.gitignore`):

```bash
# .env.example (хранится в репозитории)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rag_soc
DB_USER=rag_user
DB_PASSWORD=change_me

VECTOR_DB_HOST=localhost
VECTOR_DB_PORT=6333

KAFKA_BOOTSTRAP_SERVERS=localhost:9092

REDIS_HOST=localhost
REDIS_PORT=6379

LLM_MODEL=llama-3-8b-q4
GPU_DEVICE=cuda:0
```

---

### 3.4 Kubernetes

**ConfigMap (несекретные настройки):**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: rag-soc-config
  namespace: staging
data:
  RAG_MODE: "balanced"
  RAG_MAX_CHUNKS: "10"
  CACHE_TTL: "3600"
```

---

> 🔺 **Tier 2/3:** Vault / SOPS обязателен

---

## 4. Логирование и трассировка

### 4.1 Уровни логирования (строго)

| Уровень | Использование | PROD разрешено |
|---------|---------------|----------------|
| `ERROR` | Критические ошибки, требующие вмешательства | ✅ Да |
| `WARN` | Проблемы без нарушения workflow (fallback, retry) | ✅ Да |
| `INFO` | Бизнес-события (индексация, запрос, ответ) | ✅ Да |
| `DEBUG` | Подробные данные (промпты, эмбеддинги) | ❌ Нет (только TEST) |

---

### 4.2 JSON логирование (обязательно)

Все сервисы обязаны выводить логи в JSON формате.

**Обязательные поля:**

```json
{
  "timestamp": "2026-03-25T10:30:00.123Z",
  "level": "INFO",
  "service": "rag-soc-orchestrator",
  "trace_id": "a1b2c3d4e5f6",
  "span_id": "g7h8i9j0k1l2",
  "user_id": "user-123",
  "message": "RAG request processed",
  "duration_ms": 1234
}
```

---

### 4.3 Python пример

```python
import json
import logging
import sys

class JSONFormatter(logging.Formatter):
    def format(self, record):
        log_entry = {
            "timestamp": self.formatTime(record),
            "level": record.levelname,
            "service": "rag-soc-converter",
            "message": record.getMessage(),
            "trace_id": getattr(record, "trace_id", None),
        }
        return json.dumps(log_entry)

handler = logging.StreamHandler(sys.stdout)
handler.setFormatter(JSONFormatter())
logging.basicConfig(handlers=[handler], level=logging.INFO)
```

---

### 4.4 Распределенная трассировка (OpenTelemetry)

**Обязательные требования:**
- Каждый сервис инициализирует OpenTelemetry Tracer
- `trace_id` и `span_id` передаются в заголовках (W3C Trace-Context)
- Интеграция с Jaeger

**Заголовки:**
- `traceparent: 00-<trace-id>-<span-id>-<flags>`

---

### 4.5 Healthcheck и Readiness (обязательно)

Каждый сервис, предоставляющий API, обязан реализовать два endpoint:

| Endpoint  | Назначение                               | Использование              |
| --------- | ---------------------------------------- | -------------------------- |
| `/health` | Проверка, что сервис жив (liveness)      | Kubernetes liveness probe  |
| `/ready`  | Проверка готовности к обработке запросов | Kubernetes readiness probe |

---

#### 4.5.1 `/health` (Liveness Probe)

**Назначение:**

* Проверяет, что процесс жив
* НЕ проверяет внешние зависимости

**Требования:**

* всегда быстрый (< 50 ms)
* не делает сетевых вызовов
* не обращается к БД / Kafka / Redis

**Пример ответа:**

```json
{
  "status": "ok"
}
```

---

#### 4.5.2 `/ready` (Readiness Probe)

**Назначение:**

* Проверяет, может ли сервис обрабатывать запросы

**Обязательно проверяет:**

* DB (если используется)
* Redis (если используется)
* Kafka (если используется)
* внешние API (опционально, с timeout)

---

#### Минимальный ответ:

```json
{
  "status": "ready"
}
```

---

#### При проблемах:

```json
{
  "status": "not_ready",
  "reason": "redis_unavailable"
}
```

---

#### 4.5.3 Пример (Python / FastAPI)

```python
from fastapi import FastAPI
import asyncio

app = FastAPI()

@app.get("/health")
def health():
    return {"status": "ok"}

@app.get("/ready")
async def ready():
    try:
        # пример: проверка Redis
        await check_redis()
        
        # пример: проверка DB
        await check_db()

        return {"status": "ready"}
    except Exception as e:
        return {
            "status": "not_ready",
            "reason": str(e)
        }
```

---

#### 4.5.4 Kubernetes probes (обязательно)

```yaml
livenessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /ready
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 5
```

---

#### 4.5.5 Правила (критично)

❌ Запрещено:

* использовать `/health` для проверки БД
* делать тяжёлые операции в `/ready`
* возвращать 200 при неготовности

✅ Обязательно:

* `/ready` возвращает **HTTP 503**, если не готов
* таймаут проверки зависимостей ≤ 1 сек
* логировать причины `not_ready`

---

#### 4.5.6 Минимальные проверки по типу сервиса

| Сервис       | Что проверять в `/ready`   |
| ------------ | -------------------------- |
| API Gateway  | downstream сервисы (ping)  |
| Search       | Elasticsearch + Vector DB  |
| Generator    | LLM доступность            |
| Orchestrator | Redis + Search + Generator |
| Indexer      | Kafka + Vector DB          |

---

#### 🔺 Tier 2 / Tier 3 расширение

| Tier   | Расширение                                            |
| ------ | ----------------------------------------------------- |
| Tier 2 | degraded mode (частичная готовность)                  |
| Tier 3 | dependency graph health + circuit breaker integration |

---

## 5. Качество кода

### 5.1 Комментирование

| Тип | Обязательность | Пример |
|-----|---------------|--------|
| Public API (функции/методы) | ✅ Обязательно | Docstring с описанием, параметрами, возвратом |
| Сложная бизнес-логика | ✅ Обязательно | Пояснение алгоритма |
| Закомментированный код | ❌ Запрещено | Удалять при мерже |
| TODO/FIXME | ⚠️ Ссылка на issue | `# TODO(RAG-42): implement semantic chunking` |

---

### 5.2 Линтеры

**Python:**

- `ruff` — линтер + форматтер
- `mypy` — статическая типизация без `--strict` режима

```yaml
- ruff
- mypy
```

---

**Java/Kotlin:**
- `ktlint` — Kotlin линтер
- `spotless` — форматтер

> 🔺 **Tier 2/3:** strict typing обязателен

---

### 5.3 Типизация

**Python (mypy --strict):**
- Все функции аннотированы типами
- Запрещено `Any` без комментария с обоснованием
- Использовать `TypedDict` для структур данных

---

## 6. Тестирование

### 6.1 Типы тестов и требования

| Тип | Инструмент | Минимальное покрытие | Где запускается |
|-----|------------|---------------------|-----------------|
| Unit tests | pytest (Python), JUnit (Java), Jest (React) | 80% бизнес-логики | CI (каждый PR) |
| Integration tests | pytest + testcontainers | Критические пути | CI (merge to main) |
| Contract tests | schemathesis (OpenAPI) | Все эндпоинты | CI (staging) |
| E2E functional | pytest + requests | Основные сценарии | CI (staging deploy) |
| E2E quality | pytest + RAGAS | Полнота, точность | CI (nightly / manual) |
| Performance | k6 / locust | p95 latency, throughput | Nightly (staging) |
| Security (SAST) | Semgrep / SonarQube | Критические уязвимости | CI (каждый PR) |
| Security (DAST) | OWASP ZAP | OWASP Top 10 | CI (staging deploy) |

---

> ⚠️ **MVP компромисс:** можно начать с unit + 1–2 e2e

> 🔺 **Tier 2/3:** полный набор обязателен

---

## 7. CI/CD Pipeline

### 7.1 Платформа CI/CD 

**Выбранная платформа:** GitHub Actions (self-hosted runners в K8s)

**Обоснование выбора:**
- Нативная интеграция с GitHub (репозитории уже там).
- 20,000+ готовых actions (быстрее настройка).
- Self-hosted runners в K8s решают лимиты hosted (2000 мин/мес).
- OIDC-интеграция с K8s (без долгоживущих kubeconfig).
- Проще workflow для команды (PR → Actions → Deploy).
- Меньше операционных затрат (нет GitLab CE: экономия 4 CPU, 8 GB RAM + 0.1 FTE).

**Альтернативы:** GitLab CI (отклонено — требует синхронизации с GitHub, дополнительный компонент GitLab CE, сложнее workflow).

---

### 7.3 Пример `.gitlab-ci.yml` (фрагмент для `rag-soc-core`)

См. полный код в **п.17 (CI/CD Pipeline Definition)**, раздел 3.

---

### 7.2 Стадии пайплайна (GitHub CI)

```yaml
stages:
  - lint
  - unit-test
  - integration-test
  - sast
  - contract-test
  - build
  - sca
  - deploy-staging
  - dast
  - e2e-test
  - quality-test
  - load-test-nightly  # по расписанию
  - deploy-prod
```

---

> 🔺 **Tier 2/3:** обязательный full pipeline + rollback

---

### 7.4 Quality Gates

| Стадия | Задачи | Инструменты | Порог блокировки |
|--------|--------|-------------|------------------|
| `lint` | ruff (lint + format), mypy (strict typing) | ruff, mypy | ❌ Блокировка PR |
| `unit-test` | Unit-тесты (pytest) | pytest | ❌ Блокировка мержа (coverage < 50%) |
| `integration-test` | Integration-тесты (testcontainers) | pytest, Docker | ❌ Блокировка деплоя в staging |
| `sast` | SAST (Semgrep) | Semgrep | ⚠️ Warning (критические → блокировка после 3 раз) |
| `build` | Сборка Docker-образов, push в Harbor | Docker, Harbor | ❌ Блокировка деплоя |
| `sca` | SCA (pip-audit) | pip-audit | ⚠️ Warning (критические → блокировка после 3 раз) |
| `contract-test` | Контрактные тесты | schemathesis | ❌ Блокировка мержа |
| `deploy-staging` | Helm-деплой в namespace `staging` | Helm, K8s | — |
| `dast` | DAST (OWASP ZAP) | OWASP ZAP | ❌ Блокировка prod (High/Critical) |
| `e2e-test` | E2E Functional тесты | pytest, requests | ❌ Блокировка prod |
| `quality-test` | E2E Quality (RAGAS metrics) | pytest, ragas | ❌ Блокировка prod (faithfulness < 0.8) |
| `deploy-prod` | Canary-деплой (10% → 50% → 100%) с manual approval | Helm, Istio | — (manual approval обязателен) |

---

> 🔺 **Tier 2/3:** обязательный Блокировка мержа SAST/SCA warning

### 7.5 Локальная разработка и отладка pipeline

**Инструменты:**
- `act` — эмуляция GitHub Actions локально.

**Команды:**
```bash
# Запуск конкретного job
act -j test-unit

# Запуск всех job стадии lint
act -j lint-ruff -j lint-mypy
```

**Установка:**
```bash
curl -s https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash
```

---

### 7.6 Кэширование и артефакты

**Кэширование:**
- `pip` кэш: `/root/.cache/pip` (ключ по `requirements.txt`).
- `Gradle` кэш: `/root/.gradle/caches` (ключ по `*.gradle.kts`).
- Docker layers: через `--cache-from` в Harbor.

**Артефакты:**
- Отчёты тестов (JUnit XML) — 30 дней.
- Покрытие кода (Cobertura) — 30 дней.
- Security-отчёты (SARIF, JSON) — 90 дней.
- RAGAS-метрики (JSON) — 1 год (Object Storage).

---

> 🔺 **Tier 2/3:** Добавить параллельный запуск тестов (matrix strategy), оптимизация кэширования Docker layers, интеграция с Argo CD для GitOps.

---

### 8.2 Пример

```python
from prometheus_client import Counter

requests = Counter('requests_total', 'Total requests')
```

---

> 🔺 **Tier 2/3:** Расширение метрик, полный RAG observability (retrieval, LLM, GPU), аудит и история запросов

---

### 8.3 Логика MVP

* Логи → Loki
* Метрики → Prometheus
* Трейсы → опционально

---

### 8.4 Алертинг

| Alert | Condition | Severity | Action |
|-------|-----------|----------|--------|
| HighLatency | `rag_soc_request_duration_seconds{quantile="0.95"} > 45` | Warning | Email |
| CriticalLatency | `rag_soc_request_duration_seconds{quantile="0.95"} > 60` | Critical | Email |
| HighErrorRate | `rate(rag_soc_errors_total[5m]) > 0.05` | Warning | Email |
| HighNoAnswerRate | `rag_soc_no_answer_total{mode="balanced"}[1h] > 0.10` | Warning | Email |
| CanaryQualityDegradation | `ragas_faithfulness{version="canary"} < 0.7` | Critical | **Автоматический rollback canary** |
| CanaryHighErrorRate | `rate(rag_soc_errors_total{version="canary"}[5m]) > 0.05` | Critical | **Автоматический rollback canary** |

---

> 🔺 **Tier 2/3:** Расширение алертов

---

 
### 8.5 Дашборды (Grafana)

**Обязательные дашборды:**

| Дашборд | Аудитория | Ключевые панели |
|---------|-----------|-----------------|
| **RAG SOC Overview** | Все | RPS, latency p95, error rate |
| **Contract Tests Dashboard** | Разработчики | Процент прохождения контрактных тестов по сервисам |
---

> 🔺 **Tier 2/3:** Расширение дашбордов

---

## 9. Заключение

Документ определяет **минимально достаточные стандарты для MVP**.

> 🔺 **Tier 2/3:** Настоящий документ обязателен к исполнению для всех членов команды разработки. Нарушения стандартов должны быть зафиксированы на Code Review и исправлены до мержа.

Цель:

* не тормозить разработку
* сохранить управляемость системы

---

### 🚀 Обязательное развитие

| Tier   | Что добавить                               |
| ------ | ------------------------------------------ |
| Tier 2 | strict typing, full CI/CD, tracing         |
| Tier 3 | SLO, SLA, advanced observability, security |

---

**Контакты:**

* Архитектор: [name]
* Tech Lead: [name]
* Security Lead: [name]

---

| **Версия** | **Дата**   | **Автор** | **Изменения**                                                                                                                    |
|-----------|------------| --------- |----------------------------------------------------------------------------------------------------------------------------------|
| 0.1       | 24.03.2026 | Точенов   | Начальная версия                                                                                                                 |
| 1.0       | 25.03.2026 | Точенов   | Полная версия                                                                                                                    |
| 1.1       | 25.03.2026 | Точенов   | MVP сокращение                                                                                                                   |
| 1.2       | 29.03.2026 | Точенов   | Изменено описание струкуры папок проекта                                                                                         |
| 1.3       | 07.04.2026 | Точенов   | Изменен CI/CD на GitHub CI, репозиторий изменен на monorepo, добавлены load testing и contract testing, добавлен rollback prompt |
---
